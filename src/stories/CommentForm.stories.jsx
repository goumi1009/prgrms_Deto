import CommentForm from '@components/domain/CommentForm';

export default {
  title: 'Component/CommentForm',
  component: CommentForm,
  argTypes: {},
};

export const Default = (args) => <CommentForm {...args} />;
